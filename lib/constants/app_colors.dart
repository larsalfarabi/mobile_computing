import 'package:flutter/material.dart';

class AppColors {
  // Warm Editorial Minimalist color palette based on user reference
  static const background = Color(0xFFF3F2EC);    // Warm Matte Beige
  static const primary = Color(0xFF6B7F43);       // Muted Olive Green
  static const surface = Color(0xFFFFFFFF);       // Pure White Card Surface
  static const border = Color(0xFFD1CFC7);        // Muted Border Grey
  static const textPrimary = Color(0xFF1E2019);   // Soft Charcoal
  static const textSecondary = Color(0xFF767773); // Muted Slate Grey
  static const accentLight = Color(0xFFE2E1D9);   // Light warm beige
}
